function obterNumeroTotalDeNotas(notas) {
  let total = notas.length 
  return total
}

function obterPrimeiraNota(notas) {
 let primeira = notas[0]
 return primeira

}

function obterUltimaNota(notas) {
 let ultima = notas[notas.length - 1]
 return ultima
}
